import React, {Component} from 'react';
import {Text, View} from 'react-native';
import Baslik from './Header/Baslik';

export default class index_header extends Component {
  render() {
    return (
      <View>
        <Baslik />
      </View>
    );
  }
}
